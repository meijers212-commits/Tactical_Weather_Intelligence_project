apiVersion: v1
kind: Service
metadata:
  name: headless-svc-mySql
  labels:
    app: mySql
spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - port: 3306
    name: websql
  selector:
    app: mySql